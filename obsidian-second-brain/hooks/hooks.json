{
  "description": "Obsidian Second Brain - vault-aware context hooks. The unconditional SessionStart banner moved to rhize-context-manager/hooks/session-disclosure.js (2026-08-09, Phase 3 banner consolidation) — it surfaces this plugin's skills only when a `.obsidian/` vault is detected, instead of on every session. PreToolUse/PostToolUse hint hooks are unchanged.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/vault-write-hint.py",
            "timeout": 3
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/vault-read-hint.py",
            "timeout": 3
          }
        ]
      }
    ]
  }
}
