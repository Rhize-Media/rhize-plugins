{
  "mcpServers": {
    "slack": {
      "command": "npx",
      "args": ["slack-mcp-server"],
      "env": {
        "SLACK_TEAM_ID": "${SLACK_TEAM_ID}",
        "KEY_FILE_PATH": "/etc/keys/mine.pem"
      }
    }
  }
}
