{
  "description": "Project Launcher - artifact-aware context hooks. The unconditional SessionStart banner moved to rhize-context-manager/hooks/session-disclosure.js (2026-08-09, Phase 3 banner consolidation) — a single stack-aware disclosure surface replaces this plugin's per-session command menu. The PreToolUse vault hint hook is unchanged.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/launcher-vault-hint.py",
            "timeout": 3
          }
        ]
      }
    ]
  }
}
