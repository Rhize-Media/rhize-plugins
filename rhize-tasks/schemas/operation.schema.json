{
  "$schema": "https://rhize.media/schemas/rhize-tasks/format-assertion-2020-12.meta.json", "title": "OperationV1", "type": "object", "additionalProperties": false,
  "required": ["schemaVersion", "id", "planRevision", "kind", "targetSystem", "targetId", "payload", "idempotencyKey", "approval", "preconditionRevision", "retryState", "createdAt"],
  "properties": {"schemaVersion": {"const": 1}, "id": {"type": "string", "minLength": 1}, "planRevision": {"type": "integer", "minimum": 1}, "kind": {"enum": ["reminder_upsert", "reminder_complete", "reminder_delete", "calendar_upsert", "calendar_delete", "jira_assign", "jira_transition", "jira_comment", "jira_create", "provisional_link", "urgent_displacement", "scope_expand"]}, "targetSystem": {"enum": ["jira", "calendar", "reminders", "local"]}, "targetId": {"type": "string", "minLength": 1}, "payload": {"type": "object", "additionalProperties": false, "$comment": "Task 4 replaces this empty closed object with closed per-kind payload branches."}, "idempotencyKey": {"type": "string", "pattern": "^[0-9a-f]{64}$"}, "approval": {"enum": ["required", "approved", "applied", "rejected"]}, "preconditionRevision": {"type": ["string", "null"]}, "retryState": {"enum": ["pending", "safe_retry", "reconciliation_required", "applied", "failed"]}, "createdAt": {"type": "string", "format": "date-time"}}
}
