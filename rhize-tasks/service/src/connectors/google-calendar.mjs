import {connectorError, normalizeError, unsupported} from './http.mjs';

export function createGoogleCalendarConnector({readCalendarIds, focusCalendarId, credentials, transport, now = () => new Date(), redactOutsideTitles = true} = {}) {
  if (!Array.isArray(readCalendarIds) || !focusCalendarId || !credentials?.get || typeof transport !== 'function') throw new TypeError('invalid Google Calendar connector configuration');
  const readable = new Set(readCalendarIds);
  const object = value => value !== null && typeof value === 'object' && !Array.isArray(value);
  function bodyOf(response) { if (!object(response?.body)) throw connectorError('malformed_response'); return response.body; }
  function eventResult(response) { const body = bodyOf(response); if (typeof body.id !== 'string' || typeof body.etag !== 'string') throw connectorError('malformed_response'); return body; }
  async function normalized(action, {afterWrite = false} = {}) { try { return await action(); } catch (error) { throw normalizeError(error, {afterWrite}); } }
  async function token() { try { const [client_id, client_secret, refresh_token] = await Promise.all(['client-id', 'client-secret', 'refresh-token'].map(account => credentials.get('media.rhize.tasks.google', account))); const response = await transport({url: 'https://oauth2.googleapis.com/token', method: 'POST', headers: {'content-type': 'application/x-www-form-urlencoded'}, body: new URLSearchParams({client_id, client_secret, refresh_token, grant_type: 'refresh_token'}).toString()}); if (!response || response.status < 200 || response.status >= 300 || !response.body || typeof response.body.access_token !== 'string') throw connectorError('authorization', {status: response?.status}); return response.body.access_token; } catch (error) { throw normalizeError(error); } }
  async function request(path, options = {}) { try { const access = await token(); const response = await transport({url: `https://www.googleapis.com/calendar/v3${path}`, method: options.method ?? 'GET', headers: {authorization: `Bearer ${access}`, ...(options.body ? {'content-type': 'application/json'} : {})}, body: options.body ? JSON.stringify(options.body) : undefined}); if (!response || response.status < 200 || response.status >= 300) throw connectorError(response?.status === 401 || response?.status === 403 ? 'authorization' : 'http', {status: response?.status, retryable: response?.status >= 500 || response?.status === 429}); return response; } catch (error) { throw normalizeError(error, {afterWrite: options.method && options.method !== 'GET'}); } }
  async function events(calendarId, extra = {}) { const all = []; const seen = new Set(); let pageToken; const keyQuery = Object.hasOwn(extra, 'privateExtendedProperty'); for (let count = 0; count < 100; count += 1) { const qs = new URLSearchParams({singleEvents: 'true', orderBy: 'startTime', ...(keyQuery ? {} : {timeMin: now().toISOString()}), maxResults: '250', ...extra}); if (pageToken) qs.set('pageToken', pageToken); const body = bodyOf(await request(`/calendars/${encodeURIComponent(calendarId)}/events?${qs}`)); if (!Array.isArray(body.items) || body.items.some(item => !object(item))) throw connectorError('malformed_response'); all.push(...body.items); pageToken = body.nextPageToken; if (pageToken !== undefined && pageToken !== null && typeof pageToken !== 'string') throw connectorError('malformed_response'); if (!pageToken) return all; if (seen.has(pageToken)) throw connectorError('pagination_loop'); seen.add(pageToken); } throw connectorError('pagination_limit'); }
  const api = {
    async health() { return normalized(async () => { await request('/users/me/calendarList?maxResults=1'); return {ok: true}; }); },
    async discover() { return normalized(async () => { const all = []; const seen = new Set(); let pageToken = ''; for (let count = 0; count < 100; count += 1) { const body = bodyOf(await request(`/users/me/calendarList?${pageToken ? `pageToken=${encodeURIComponent(pageToken)}` : ''}`)); if (!Array.isArray(body.items) || body.items.some(item => !object(item) || typeof item.id !== 'string')) throw connectorError('malformed_response'); all.push(...body.items); pageToken = body.nextPageToken ?? ''; if (typeof pageToken !== 'string') throw connectorError('malformed_response'); if (!pageToken) return all.filter(item => readable.has(item.id) || item.id === focusCalendarId); if (seen.has(pageToken)) throw connectorError('pagination_loop'); seen.add(pageToken); } throw connectorError('pagination_limit'); }); },
    async readSnapshot() { return normalized(async () => (await Promise.all([...readable].map(async calendarId => (await events(calendarId)).map(event => ({id: event.id, calendarId, revision: event.etag, start: event.start?.dateTime ?? event.start?.date, end: event.end?.dateTime ?? event.end?.date, title: calendarId === focusCalendarId || !redactOutsideTitles ? event.summary ?? '' : '', description: calendarId === focusCalendarId || !redactOutsideTitles ? event.description ?? '' : ''}))))).flat()); },
    async applyOperation(operation) { return normalized(async () => { if (!['calendar_upsert', 'calendar_delete'].includes(operation?.kind)) unsupported(); if (operation.kind === 'calendar_upsert') { if (operation.payload.calendarId !== focusCalendarId) throw connectorError('out_of_scope'); if (!operation.targetId) { const old = (await events(focusCalendarId, {privateExtendedProperty: `rhizeOperationKey=${operation.idempotencyKey}`}))[0]; if (old) return {externalId: old.id, revision: old.etag}; } const body = {summary: operation.payload.title, description: operation.payload.description, start: {dateTime: operation.payload.start}, end: {dateTime: operation.payload.end}, extendedProperties: {private: {rhizeOperationKey: operation.idempotencyKey}}}; try { const event = eventResult(await request(`/calendars/${encodeURIComponent(focusCalendarId)}/events${operation.targetId ? `/${encodeURIComponent(operation.targetId)}` : ''}`, {method: operation.targetId ? 'PUT' : 'POST', body})); return {externalId: event.id, revision: event.etag}; } catch (error) { if (error.ambiguous) { const old = (await events(focusCalendarId, {privateExtendedProperty: `rhizeOperationKey=${operation.idempotencyKey}`}))[0]; if (old) return {externalId: old.id, revision: old.etag}; } throw error; } } await request(`/calendars/${encodeURIComponent(focusCalendarId)}/events/${encodeURIComponent(operation.targetId)}`, {method: 'DELETE'}); return {externalId: operation.targetId, revision: operation.idempotencyKey}; }, {afterWrite: true}); },
    async findByExternalId(externalId) { return normalized(async () => { const matches = await events(focusCalendarId, {privateExtendedProperty: `rhizeOperationKey=${externalId}`}); if (matches[0]) return {revision: matches[0].etag}; try { const event = eventResult(await request(`/calendars/${encodeURIComponent(focusCalendarId)}/events/${encodeURIComponent(externalId)}`)); return {revision: event.etag}; } catch (error) { if (error.status === 404) return null; throw error; } }); },
  };
  return api;
}
