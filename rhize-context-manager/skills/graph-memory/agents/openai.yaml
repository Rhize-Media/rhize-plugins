interface:
  display_name: "Graph Memory"
  short_description: "Govern graph and decision projections"
  default_prompt: "Use $graph-memory to validate a source-bound Graphify artifact or preview a consequential decision through the offline governed contract without touching live Neo4j."
