[pytest]
# These are executable benchmark workspaces, not repository test modules. The
# harness copies them to isolated run directories and grades them with unittest.
norecursedirs = evals/parallel-agent-skills/tasks
