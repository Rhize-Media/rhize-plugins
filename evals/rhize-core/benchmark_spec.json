{
  "schema_version": 1,
  "component": "rhize-core",
  "arms": {
    "arm_a": {
      "id": "greenfield",
      "definition": "No confirmed exact pre-plugin baseline exists yet -- rhize-core is a repo-shape split of rhize-ops' pre-existing setup engine into its own plugin, not a wholly new capability. Record no invented incumbent identity until a real one is confirmed; the first verified production version of the split can become a future frozen baseline."
    },
    "arm_b": {
      "id": "plugin_path",
      "definition": "Run the same pinned cases through /rhize-core:setup and record the invoked phase/subcommand."
    }
  },
  "common_metrics": ["correctness", "accuracy", "routing_precision", "routing_recall", "input_tokens", "output_tokens", "cache_tokens", "elapsed_latency_ms", "tool_calls", "follow_up_reads", "corrections_rework", "failures_refusals"],
  "receipt_required_fields": ["run_id", "arm", "case_id", "baseline_commit", "plugin_commit", "model", "model_settings", "started_at", "completed_at", "metrics"],
  "live_run": {
    "status": "pending",
    "arm_ran": "none",
    "reason": "No live or natural Arm A/Arm B benchmark was run because this task forbids paid and network calls, and rhize-core has no confirmed baseline yet.",
    "immediate_local_gate": "python3 evals/rhize-core/run_evals.py"
  },
  "results": []
}
